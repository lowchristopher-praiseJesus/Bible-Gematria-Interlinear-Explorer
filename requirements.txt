# Core Flask dependencies
# Upper bounds match the known-good local set (Flask 3.0 / Flask-Caching 2.4 /
# Flask-Cors 4.0). Flask-Caching 2.5.0 breaks the lazy 'filesystem' backend
# import used by CACHE_TYPE='filesystem', so it is capped below.
flask>=2.0.0,<3.1
flask-caching>=1.10.0,<2.5
flask-cors>=3.0.0,<5.0
dataset>=1.5.0
requests>=2.28.0            # myproject.py: /api/bible-chat/* reverse proxy

# Chatbot dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
httpx>=0.27.0
pydantic>=2.9.0
python-multipart>=0.0.12

# Optional: For AI responses via Ollama cloud
# httpx is used for Ollama API calls

# Study wiki dependencies
markdown>=3.7
